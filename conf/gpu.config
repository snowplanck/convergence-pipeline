// GPU profile — adds GPU resources to heavy processes
params {
    gpu_enabled = true
}
process {
    withLabel: 'gpu_cheap'    { accelerator = 1; gpu = true }
    withLabel: 'gpu_expensive'{ accelerator = 1; gpu = true }
}
