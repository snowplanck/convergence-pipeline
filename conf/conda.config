// Conda profile — per-tool environments resolved via conda
conda {
    enabled         = true
    useMamba        = true
    cacheDir        = "$HOME/.conda/envs_cache"
    createTimeout   = '30 min'
}
